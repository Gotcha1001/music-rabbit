// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
  // ← Paste ALL of your previous config options here
  // Example (replace with your actual config):
  experimental: {
    // your experimental flags
  },
  images: {
    // your image domains, etc.
  },
  // ... any other settings you had
};

module.exports = nextConfig;


